{
  "name": "sparrow-react",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "api": "hotnode app-server",
    "start": "rstool-build --server",
    "dev": "rm -rf ./dist && rstool-build --env dev --cdn --hash --devtool source-map",
    "uat1": "rm -rf ./dist && rstool-build --env uat1 --cdn --hash --devtool source-map",
    "stg": "rm -rf ./dist && rstool-build --env stg --cdn --hash",
    "prd": "rm -rf ./dist && rstool-build --env prd --cdn --hash"
  },
  "author": "Land",
  "license": "MIT",
  "dependencies": {
    "antd-mobile": "^0.9.14",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-router": "^3.0.2"
  },
  "devDependencies": {
    "glob": "^7.1.1",
    "react-hot-loader": "^1.3.1",
    "rstool-build": "^0.1.21"
  }
}
